query findByProximity(
    $latitude: Float!
    $longitude: Float!
  ){
  findByProximity(
    latitude: $latitude
    longitude: $longitude
  ) {
    _id
    name
    address
    matrix(
      latitude: $latitude
      longitude: $longitude
    ) {
      distance
      duration
      mode
    }
  }
}
